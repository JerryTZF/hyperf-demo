version: "3.5"

services:
  # 本地开发(Mac环境)
  gift-point-server:
    build: .
    container_name: gift-point-server
    ports:
      - "9500:9501"
    networks:
      - proxy
    volumes:
      - $PWD:/opt/www
networks:
  proxy:
    external: true